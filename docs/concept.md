# QuuBee コンセプト

QuuBee の現在のコンセプト（魂）を記述する。実装の詳細は [README.md](../README.md) /
[CLAUDE.md](../CLAUDE.md) / [docs/structure.md](structure.md)、進捗と当初構想からの変遷は
[CHANGELOG.md](../CHANGELOG.md) / [TODO.md](../TODO.md) を参照。

---

## ミッション

> **QuuBee は、PC-98 のフリーソフト文化を、罪悪感なく継承・再体験できるブラウザプレイヤー。**
>
> NEC BIOS も MS-DOS も使わず、書庫ファイルをドロップすれば、あの頃のように、すぐ遊べる。

タグライン候補:
- 「書庫があれば、すぐ遊べる。あの頃のまま。」
- 「PC-98 フリーソフト文化を、罪悪感なく。」
- 「機械じゃなく、ソフトを継承する。」

---

## 3 本の柱

### ① 著作権クリーン（構造的な独自性）
ユーザーが触れるスタック全体が著作権クリーン:
- **BIOS**: NEC ROM 不使用。NP2kai 内蔵の最小 BIOS（`nosyscode` + C ハンドラ）+ 合成 BIOS。
- **DOS**: MS-DOS 不使用。INT 21h を C 側で **HLE 模倣**（ミニ DOS ローダ）。
- **フォント**: NEC フォント ROM 不使用。`font.bmp`（修正 BSD）で代替。帰属は [CREDITS.md](../CREDITS.md)。
- **コア**: NP2kai（MIT）を **LLE** で利用。

→ **知財保護と互換性を両立**。結果として **「URL を開くだけ」で合法に成立する、公開ホスティング可能な
PC-98 系プレイヤー**になり得る（実機 BIOS/DOS を要求する他のエミュは公開配布が法的に困難）。

### ② お手軽さ（当時の流儀の再現）
当時のフリーソフト体験は「Vector/Nifty/BBS で書庫を入手 → LHA で解凍 → EXE を叩く → 即遊ぶ」。
QuuBee の「**書庫をドロップ → 即プレイ**」はこれそのもの。ディスクイメージ作成も DOS インストールも
config.sys いじりも不要。「**機械を丸ごと立ち上げる**」のではなく「**ソフトを動かす**」。

### ③ 敬意（罪悪感なく、を支える仕組み）
- **フリーソフト限定**（商用ゲームを動かす公開経路は作らない）。
- **原作者クレジット・連絡先・readme をそのまま継承**（テキストビューアで提示）。
- **Tier-1 内蔵は許諾ベース**（原作者の許可を得たフリーソフトのみ）。
- **第三者アセットのライセンスを正しく帰属**（同梱フォント等、[CREDITS.md](../CREDITS.md)）。

> 削除依頼（オプトアウト）窓口は置かない。QuuBee はゲームを配布・ホストしない（Tier-1 は許諾ベース、
> Tier-2 はユーザー自身のファイル）ため、「削除依頼」はホスティング事業者の概念で QuuBee には当てはまらない。
> リストからの除外等が要れば連絡ベースで個別対応すれば足りる。

---

## 「快適化」の意味

QuuBee の「快適化」は **「ハードの制約からの解放」を CPU 速度だけでなくコンテンツアクセス全般へ広げる**こと:

- **CPU**: クロック倍率 x42 近傍（VSync/タイマ依存ゲームは無調整で快適化。空ループ依存は要調整）。
- **ディスク入れ替え不要**: 複数ディスクのフリーソフトも全ファイルを 1 ディレクトリに展開して起動
  （当時の入れ替え地獄が消える ＝ 作者が本来させたくなかったはずの体験の解放）。
- **即起動・即解凍**: FDD 待ちなし、起動シーケンス・スキップ。

互換性は手段、快適化は機能、**文化継承が目的（魂）**。

---

## 入口ポリシー（窓口）

### 正面玄関＝書庫ファイル一本
- **対応: `.lzh`（当時の本流）+ `.zip`（Vector/Internet Archive の現代チャネル）**。RAR は対象外。
- **複数書庫の同時展開**: 複数の `.lzh`/`.zip` を一度に投入 → すべて同じ仮想 HD ディレクトリ
  （Emscripten FS `/run/`）へ展開 ＝ **デジタル版「HD にインストール」**。多ディスクの大作フリー RPG 等に対応。
- **パッチ（2 形態に対応）**:
  - (a) **ファイル上書き型** — 「本体書庫 + パッチ書庫」を順に展開（last-wins、展開順=投入順、並べ替え可）。
  - (b) **パッチャ EXE 型** — 「`PATCH.EXE` を実行して対象のバイナリを書き換える」もの。QuuBee は実 DOS
    プログラムを動かせるので**パッチャ EXE をそのまま実行**でき、`/run/` のファイルを書き換える（INT 21h の
    open/seek/write）。パッチ後の `/run/` 状態を `.qb` に保存すれば「パッチ済み完成形」になる。

### ディスクイメージの扱い（赤線＝ユーザーのディスクイメージからのブート）
ディスクイメージ対応そのものは残す。引くべき赤線は **「ユーザーが持ち込んだディスクイメージから *ブート*
させること」**（＝商用ゲームの丸ごとダンプ／機械丸ごと起動の経路）。これを公開 UI に置かない。
- **内部利用**: ミニ DOS ローダ自身が `loader.d88` をマウントして起動する（FDD/ブート経路が書庫プレイの土台）。
  コアは常にディスクイメージ能力を保持する ＝「窓口だけ絞る、コアはそのまま」。
- **ファイルコンテナとしての利用（任意・将来）**: フリーソフトがディスクイメージに*ファイルとして*入っている
  場合（VZ Editor 等）、**ブートせず中身を `/run/` へ取り出す**経路はクリーンに提供し得る。
- **真の自己起動もの**（例: Super Depth `.fdi`）はブートが必須なので、公開では**書庫（`.lzh`）版を使う**か
  開発ビルドで扱う。

---

## プロダクト層（フロントエンドの "皮"）

重要原則: **著作権クリーンなコア（Wasm）は完成・安定。プロダクト機能はすべて JS 側**で、Wasm は触らない。
以下は順次実装する（現時点は未着手が多い）。

- **テキストビューア（読み込み専用）**: `readme.doc` / `.txt` / `.bat` 等を表示。**SJIS 自動デコード**
  （ブラウザ標準 `TextDecoder('shift_jis')`、既定 SJIS + 手動切替）、改行正規化。文書 ＝ 文化的
  アーティファクト（作者の声・主題歌・謝辞・連絡先）の継承であり、③敬意を体現。ファイル一覧兼用。
- **本棚（個人ローカルライブラリ）**: 「自分で集めた `.qb` がサムネで並ぶ本棚」。永続化は **IndexedDB**
  （全ブラウザ・Chromebook OK、既定）/ **File System Access API**（実フォルダ指定、Chrome/Chromebook が強い、上級）。
  ユーザーが自分の手元に自分のファイルを置くだけなので配布・著作権の問題は生じない。
- **サムネイル**: canvas のフレームバッファ・スナップショット（`toBlob`）。タイトル画面の自動取得 or
  ユーザー選択。`.qb` に同梱。
- **`.qb` 形式**: **オリジナル書庫ファイル** + 展開済みファイル + 設定 + セーブ + サムネ + クレジットを束ねる
  「**組み上げ済み・パッチ済み・設定込みの完成形**」。オリジナル書庫を同梱することで**出所の保全・再展開・
  検証**が可能（改変の有無を確認でき、③敬意とも整合）。コミュニティ保存フォーマットになり得る。
- **中央フィンガープリント DB は持たない**: `.qb` が自己記述的（設定・エントリ・クレジット・サムネ・原書庫内蔵）
  なので、中央 DB は不要。生書庫を初投入したときは:
  - **エントリ**: `.exe`/`.com` を**自動検出**（1 個なら自動、複数ならユーザー選択）。
  - **起動引数・クロック倍率**: readme ビューアが必要な引数を提示 → ユーザー入力 → 動いたら `.qb` 保存
    （調整知識は `.qb` が運ぶ）。
  - **同定/帰属**: 同梱 readme の継承表示でカバー。
  - コミュニティは中央 DB ではなく **`.qb` を共有**して知識を流通させる（分散・保守不要・堅牢）。
  - ※ 開発テスト用だった `db/games.json` は .bat 解釈 + エントリ自動検出で不要になり削除済み（2026-06-12）。
- **Tier-1 内蔵**: 作者公認フリーソフトの `.qb` を数本プリシード（最初から本棚に並ぶ）。

### 体験ループ
> 書庫をドロップ → **エントリ自動検出/選択**（引数が要れば readme を見て入力）→ プレイ →
> **「本棚に保存」で `.qb` 化（原書庫・サムネ・設定込み）** → 次回は**本棚のサムネをクリックで即プレイ**

---

## やらないこと

- ❌ CRT シェーダ / 走査線エフェクト（当時の質感の再現はしない）
- ❌ 起動シーケンスを演出として再現すること（凝った BIOS POST 画面など）。
  ※ 高速メモリチェック由来の一瞬の表示は速くて無害なので、あえて抑止はしない。
- ❌ エミュレータ設定 UI（CPU/RAM/サウンドボード選択）
- ❌ 機種切替（**PC-9821/NP21 に固定**）
- ❌ 詳細なキーマッピング設定 UI
- ❌ スナップショット式セーブステート（ゲーム本来のセーブ機構を使う。ただしそのセーブと本棚は永続化する）
- ❌ FM 音楽のエクスポート等の技術的機能
- ❌ ネイティブアプリ化（ブラウザのまま、PWA は可）
- ❌ 持ち込みファイルの設定手動チューニング UI
- ❌ 商用ゲームを動かす公開経路（ディスクイメージからのブートを公開 UI に置かない）
- ❌ 削除依頼（オプトアウト）窓口（配布・ホストしないため不要）

---

## 対象スコープ

- **フロッピーベース・2D・〜1998 年の PC-98 同人/フリーソフト**。カバー率見込み **80〜90%**。
- 対象外: DOS Extender（DOS/4GW, PMODE/W; DPMI host が必要）、CD-ROM（MSCDEX/ISO9660）、
  N88-BASIC ROM 依存（著作権）、商用ソフト全般、分割書庫（`.l01`/`.l02`）。
- **3D ポリゴンは対象外ではない**: PC-98 に 3D ハードは無く、当時の「3D」は全て**ソフトウェアレンダリング**。
  QuuBee は i386c + FPU(DOSBox2) を持つので CPU/FPU で普通に動く。「対象外」は欠けているサブシステム
  （DPMI/MSCDEX 等）の話で、3D は該当しない。
- 実用ソフト（一太郎/Lotus/桐 等）は技術的に動作可能だが、フリー/許諾が前提。

---

## コミュニティ / 原作者戦略

- **フリーソフト限定 + クレジット継承 + 許諾ベース Tier-1 + 第三者アセットの正しい帰属** が信頼の基盤。
- **最初の一手 = すでに動かしているゲームの作者**:
  - **おにたま氏 / onion software 様**（ザルバール、HSP）
  - **Bio 100% 様**（Super Depth）
  - 「ROM も DOS も要らずブラウザで動く、Tier-1 として保存・配信したい」は刺さる提案。
- **ZUN 氏（PC-98 東方）は夢の上澄み**。取れたら大ニュース、取れなくて当然の温度感で。

---

## 実装状況と次の一手

- **Phase 1 完了** ✓ / **Phase 2 実質完了** ✓ / **Phase 3 公式 3/4 達成** ✓（詳細は CHANGELOG/TODO）。
- **コア（著作権クリーン Wasm）は完成・安定。**
- **残り = プロダクト層**: テキストビューア / 本棚（永続化）/ サムネ / `.qb`（原書庫同梱）/ エントリ自動検出 /
  Tier-1 / 複数書庫展開・パッチ UI / デプロイ（CI + ホスティング）/ PWA。
- **個別課題（Phase 4 候補）**: Ray IV オープニング完全表示、Bio 100% 蟹味噌のテキスト面残留。

---

## UI デザイン（TBD）

未決定（後で詰める）。検討候補:
- 現状の配色は**アングラ連想**があるので再検討対象。
- 方向性候補: ①現代風の洗練、②ゲーム寄りのポップ、③当時のホームページ風デフォルト HTML
  （グレー地・センタリング・`<hr>` 区切り）。

---

## 未確定事項

- `.qb` 形式の詳細仕様（原書庫・展開物・メタの持ち方、互換パッチ同梱可否、セーブの持ち方）。
- 永続化の優先: File System Access API（手元に実ファイル）vs IndexedDB（手軽）。
- サムネ自動取得のタイミング（タイトル検出 / 一定フレーム後 / 手動）。
- マネタイズ / サステナビリティ。
- Internet Archive の取り扱い（ユーザー入手経路としての明記の是非）。
